# Template C++

Template C++ is a repository generator for C++ projects. To create a new C++
project with the template install
[Cookiecutter](https://github.com/cookiecutter/cookiecutter), execute the
following command, and follow its interactive prompts.

```console
cookiecutter --directory cpp gh:scruffaluff/templates
```

## Prompts

Cookiecutter will ask the following questions during project generation.

- **Project name** - Human-readable name for the project.
- **Project description** - Short one-line description.
- **Project repository** - Full URL of the project repository. Its domain
  determines which CI configuration is generated. Use `github.com` for GitHub
  Actions or `gitlab.com` for GitLab CI.
- **Add command line interface** - Boolean flag which integrates CLI code if
  enabled. Otherwise, configures the project as a library.

## Setup

To develop with the generated project, install [Just](https://just.systems) and
a C compiler. Then step into the project folder and execute `INIT=1 just setup`
and you are ready to code.

## Commands

The generated `justfile` provides the following recipes for development.

| Recipe        | Description                                    |
| ------------- | ---------------------------------------------- |
| `just build`  | Build project for release or test              |
| `just ci`     | Run full CI pipeline: setup, lint, test, build |
| `just format` | Format code with Clang Format and Prettier     |
| `just lint`   | Run Prettier, Clang Format, and Clang Tidy     |
| `just run`    | Run project binary (add `DEBUG=1` for LLDB)    |
| `just setup`  | Install dev tools and dependencies             |
| `just test`   | Run test suite (add `DEBUG=1` for LLDB)        |

Most recipes accept additional arguments. For example, `just format test`
formats only that folder.

## Tooling

Every generated project configures the following tools for development usage.

- [CMake](https://cmake.org): Cross-platform build system.
- [Clang Format](https://clang.llvm.org/docs/ClangFormat.html): Code formatter.
- [Clang Tidy](https://clang.llvm.org/extra/clang-tidy): Static analyzer.
- [Conan](https://conan.io): C/C++ package manager.
- [Prettier](https://prettier.io): Code formatter for JSON, Markdown, and YAML
  files.

IDE debugger configurations for VS Code (`.vscode/launch.json`) and Zed
(`.zed/debug.json`) are included.

## Layout

The following diagram shows all possible files generated by Cookiecutter. If a
file is followed by `{option: selection}`, then that path is only generated for
that context.

```
{{project_name}}
├── .clang-format
├── .clang-tidy
├── .dockerignore
├── .github  {project_repository: contains 'github.com'}
│   └── workflows
│       └── main.yaml
├── .gitignore
├── .gitlab-ci.yml  {project_repository: contains 'gitlab.com'}
├── .prettierignore
├── .prettierrc.yaml
├── .vscode
│   └── launch.json
├── .zed
│   └── debug.json
├── CMakeLists.txt
├── conanfile.txt
├── CONTRIBUTING.md
├── justfile
├── README.md
├── src
│   ├── lib.cpp
│   ├── lib.hpp
│   └── main.cpp  {project_cli: true}
└── test
    └── cli.cpp
```

## Continuous Integration

Projects generated with this template repository are configured for automated CI
on either GitHub Actions or GitLab CI, depending on the repository URL provided
during generation.

The CI pipeline runs the full `just ci` recipe across Linux, MacOS, and Windows
platforms for all code pushes. On Git tag pushes, the pipeline builds artifacts
and publishes a release with package assets. The CI pipeline supports debugging
for all jobs with Tmate.
